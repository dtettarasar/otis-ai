  <style>

    .keyword-bdge {
      font-size: 0.8rem;
      padding: 5px;
      margin: 5px;
    }

    .keyword-bdge p {
      margin:4px;
    }

  </style>
  
  <!-- Modal -->
  <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-centered">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLabel">Generate Text with AI</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <strong>What your article should be about?</strong>
          Use the fields below to tell us about your article. 

          <form action="/article/create-ai" method="post">

            <div class="form-group mt-2">
                <label for="title">Title</label>
                <input value="" required type="text" name="title" id="title" class="form-control">
            </div>
            
            <div class="form-group mt-2">
                <label for="description">Description</label>
                <textarea name="description" id="description" class="form-control"></textarea>
            </div>

            <div class="form-group mt-2 mb-2">
              <label for="keywords">Keywords</label>
              <input value="" required type="text" name="title" id="keywords" class="form-control">
            </div>

            <div class="d-flex align-items-center justify-content-evenly flex-wrap" id="keywords-container">

              <div class="badge bg-primary keyword-bdge d-flex flex-row">
                  <p>Test short</p>
                  <button type="button" class="btn-close" aria-label="Close"></button>
              </div>

              <div class="badge bg-primary keyword-bdge d-flex flex-row">
                <p>Test Keywords</p>
                <button type="button" class="btn-close" aria-label="Close"></button>
              </div>

              <div class="badge bg-primary keyword-bdge d-flex flex-row">
                <p>Test short</p>
                <button type="button" class="btn-close" aria-label="Close"></button>
            </div>

            <div class="badge bg-primary keyword-bdge d-flex flex-row">
              <p>Test short</p>
              <button type="button" class="btn-close" aria-label="Close"></button>
            </div>

            <div class="badge bg-primary keyword-bdge d-flex flex-row">
              <p>Test short</p>
              <button type="button" class="btn-close" aria-label="Close"></button>
          </div>

          <div class="badge bg-primary keyword-bdge d-flex flex-row">
            <p>Test Keywords this one is longer</p>
            <button type="button" class="btn-close" aria-label="Close"></button>
          </div>

            </div>

            <button id="add-kywrd-btn" type="button" class="btn btn-secondary mt-2">Add keywords</button>

            <button type="submit" class="btn btn-success mt-2">Generate Article</button>

          </form>

        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-danger" data-bs-dismiss="modal">Cancel</button>
          <!-- <button id="modal-validate-btn" type="button" class="btn btn-primary">Save changes</button>-->
        </div>
      </div>
    </div>
  </div>

<script>

    console.log('test');
    const addKeyWrdBtn = document.querySelector("#add-kywrd-btn");
    const addKeyWrdField = document.querySelector("#keywords");
    const keywordsContainer = document.querySelector("#keywords-container");
    const keywordsLimit = 3;

    const keywordArr = [];

    const getKeyWords = () => {
      console.log("init get keywords button");

      if (keywordArr.length < keywordsLimit) {

        keywordArr.push(addKeyWrdField.value);

        const tagPElem = document.createElement("p");
        const tagSpanElem = document.createElement("span");
        const tagTxtElem = document.createTextNode("This is a new paragraph.");

        tagSpanElem.appendChild(tagTxtElem);
        tagPElem.appendChild(tagSpanElem);
        keywordsContainer.appendChild(tagPElem);


        addKeyWrdField.value = "";

      } else {

        (console.log(`you can add a maximum of ${keywordsLimit}`));

      }

      console.log(keywordArr);
    }

    addKeyWrdBtn.addEventListener("click", getKeyWords);

</script>